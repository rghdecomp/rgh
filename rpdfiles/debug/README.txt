Note: If you see files that look the same but are not in the debug folder, it’s because they are duplicates.
The folders should remain identical, and any files that don’t appear in both are unnecessary.

!! ## debug folder is extractedfrom Eggscantfly's RGH European Debug Patch ## !!